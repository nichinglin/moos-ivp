//--------------------------------------------------------
// pNodeReporter Configuration Block (Created from Plugin)

ProcessConfig = pNodeReporter
{
  AppTick     = 2
  CommsTick   = 2
  
  VESSEL_TYPE = $(BOAT) // or {uuv,auv,ship,kayak,glider}
  platform_length    = $(BOAT_LEN) //default 8 {meters.  Range [0,inf) }
  platform_color = $(COLOR)
}
